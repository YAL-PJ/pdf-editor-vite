{
  "name": "pdf-editor-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:lcp": "npm run build && playwright test -c playwright.preview.config.ts tests/e2e/lcp.spec.ts",
    "test:e2e": "playwright test",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "immer": "^10.1.1",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^5.4.54"
  }
}
